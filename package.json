{
  "name": "@bumble/clipboard",
  "version": "0.1.2",
  "description": "Use the clipboard with ease in Chrome extensions.",
  "main": "build/bundle-cjs.js",
  "module": "build/bundle-esm.js",
  "author": "Jack and Amy Steam <jacksteamdev@gmail.com>",
  "license": "MIT",
  "repository": "bumble-org/clipboard",
  "devDependencies": {
    "@sucrase/jest-plugin": "^2.0.0",
    "eslint": "^5.11.0",
    "jest": "^24.1.0",
    "rollup": "^0.68.0",
    "sinon-chrome": "^2.3.2",
    "sucrase": "^3.9.5"
  },
  "scripts": {
    "build": "rollup -c",
    "start": "rollup -c -w",
    "test": "jest",
    "preversion": "npm run build",
    "postversion": "git push && npm publish"
  },
  "jest": {
    "transform": {
      ".(js|jsx|ts|tsx)": "@sucrase/jest-plugin"
    },
    "setupFilesAfterEnv": [
      "./tests/jest.setup.js"
    ]
  },
  "files": [
    "build"
  ],
  "dependencies": {
    "@bumble/chrome": "^0.1.1",
    "@bumble/stream": "^0.2.2"
  }
}
